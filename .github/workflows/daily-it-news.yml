name: Daily IT News

on:
  schedule:
    - cron: "0 9 * * *"
  workflow_dispatch:
    inputs:
      action:
        description: "Action from approval email"
        required: false
        default: ""
        type: string
      summary:
        description: "Current summary text"
        required: false
        default: ""
        type: string
      image:
        description: "Current image URL"
        required: false
        default: ""
        type: string

jobs:
  post-news:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Parse Inputs
        id: vars
        run: |
          echo "action=${{ github.event.inputs.action || '' }}" >> $GITHUB_OUTPUT
          echo "summary=${{ github.event.inputs.summary || '' }}" >> $GITHUB_OUTPUT
          echo "image=${{ github.event.inputs.image || '' }}" >> $GITHUB_OUTPUT

      - name: Post to Bluesky (Approved)
        if: steps.vars.outputs.action        if: steps.vars.outputs.action        if: steps.vars.outputs.action               if: steps.d t  Bl        ifes        if: steps.name:         if: steser
            st            pu            st            pu            st      io            st                      st  ll f            st     na            s
        if: steps.vars.outputs.action != 'app        isteps.vars.outp        if: steps.o_        if: sterun: |        if:python3 - <<'PY'
          import os
          import random
           rom datetime im           rom datezon    im                        rom datetime iZo                 import feedparser

                                   ht               gister.com/headlines.atom",
              "https://www.bbc.co.uk/news/technology/rss.xml",
                                                                                          rw                                                                               = datetime.now(timezone.utc)
          window = now_utc - timedelta(hours=24)
          recent_entries = []
          fallback_entry = None
          fallback_dt = None

          def entry_dt(entry):
              if getattr(entry, 'publish              if getattr(entry, 'publish              if getattr(entry, 'publish              if getattr(entry, 'publish              if getattr(entry, 'pune):
                  return datetime(*entry.updated_parsed[:6], tzinfo=timezone.utc)
              return None

          for url in FEEDS:
              feed              feese(url)
              for entry in feed.entries:
                  dt = entry_dt(entry)
                  if not dt:
                      continue
                  if dt >= window:
                      recent_entri                      recent_entri                    t is None                      recent_entri                      recent_entri                    t is None                      recent_entri                      recent_entri                    t is None    'N                      recent_entri           t, chosen_entry = (random.choice(recent_entries) if recent_entries else (fallback_dt, fallback_entry))

          summary = getattr(chosen_entry, 'summary', getattr(chosen_entry, 'description', '')) or ''
          summary = ' '.join(summary.split())
          link = getattr(chosen_entry, 'link', 'https://www.theregister.com/          link = getattr(chosen_entry, 'link', 'https://www.theregister) if chosen_dt else datetime.now(ZoneI          link = getattr(             link = getattr(chosen_rftime('%d/          link = getattr(pen(os.environ['GITHUB_ENV'], 'a') as env:
              env.write(f"ARTICLE_SUMMARY={summary}\n")
              env.write(f"ARTICLE_LINK={link}\n")
              env.write(f"ARTICLE_DATE={formatted_date}\n")
          PY

      - name: Generate AI Summary
        if: steps.vars.outputs.action != 'approve' && steps.vars.outputs.action != 'redo_image'
        run: |
          PROMPT_TEXT="You are a sarcastic British IT support columnist. Given the news summary '$ARTICLE_SUMMARY', craft a witty British-leaning recap that highlights the epic IT fail and includes at least one pun. Respond ONLY          PROMPT_TEXT="You are a sarcastic British IT support columnist. Given the news sum#tag2\", \          PROMPT_TEX    PAYLOAD=$(jq -n --arg msg "$PROMPT_TEXT" '{model: "nousresearch/hermes-3-llama-3.1-405b", messages: [{role: "user", content: $msg}], max_tokens: 200}')

          AI_RESPONSE=$(curl -s -X POST "https://openrouter.ai/api/v1/chat/completions" \
            -H "Authorization: Bearer ${{ secrets.OPENROUTER_API_KEY }}" \
            -H "Content-Type: application/json" \
            -d "$PAYLOAD")

                              E" | grep -q '"error"'; then
            echo "API Error: $AI_RESPONSE"
            exit 1
          fi

          CONTENT=$(echo "$AI_RESPONSE" | jq -r '.choices[0].message.content')
          SUMMARY_TEXT=$(echo "$CONTENT" | jq -r '.summary' 2          SUMMARY_TEXT=$(echo "$CONTENT" | jq -r '.summary' 2          SUMMARY_TEXT=$(echo "$CONTENT" | jq -r '.summary' 2          SUMMARY_TEXT=$(echo "$CONTENT" | jq -r '.summary' 2          SUMMARY_TEXT=$(echo "$CONTENT" | jq -r '.summary' 2          SUMMARY_TEXT=$(echo "$CONTENT" | jq -r '.summary' 2          SUMMARY_TEXT=$(echo "$CONTENT" | jq -r '.summary' 2          SUMMARY_TEXT=$(echo "$CONTENT" | jq -r '.summary' 2          SUMMARY_TEXT=$(echo "$CONTENT" | jq -r '.summary' 2          SUMMAENT_SUMMARY=$SUMMARY_TEXT" >> $GITHUB_ENV
          echo "HASHTAGS_TEXT=$HASHTAGS_LINE" >> $G          echo "HASHTAGS_TEXT=$HASHTAGS_LINE" >> $G          echo "HASHTANV
          echo "DATE_LINE=Date: $ARTICLE_DATE" >>          ec

      - name: Use Existing Summary for Redo Image
        if: steps.vars.outputs.action == 'redo_image'
        run: |
          echo "CURRENT_SUMMARY=${{ steps.vars.outputs.summary }}" >> $GITHUB_ENV

      - name: Generate Car      - name: Generate Car      - name: Generate Car      - name: Generate Car      - name: Generate Car      - name: Generate Car      - name: Generate Car      - name: Generate Car      - name: Generate Car      - name: Generate Car      - name: Generate Car      - name: Generate Car      - name: Generate Car      - name: Generate Car      - name: Generate Car      - name: Generate Car      - name: Generate Car      - name: Generate Car      - name: Generate Car      - name: Generate Car      - name: Generate Car      - name: Generate Car      - name: Generate Car      - name: Generate Car      ; then
            IMAGE_BASE64=$(echo "$RESPONSE" | jq -r '.result.image')
            if [ -n "$IMAGE_BASE64" ] && [ "$IMAGE_BASE64" != "null" ]; then
              IMAGE_DATA="data:image/pn              IMAGE_DATA="data:image/pn    "CURREN              IMAGE_DATA="data:image/pn             ho "IMAGE_SOURCE=Cloudflare AI" >> $GITHUB_ENV
              exit 0
            fi
          fi

                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             EY                                                               }}
                                                                               VE                                                                               VE                                                                               VE                python3 -                  import json
                  s
                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                _l                                                                                                                                                                                                                                                                                                                <h2>Daily IT News Draft</h2>
          <p><strong>Summary</strong><br/>{summary}</p>
          <p><strong>Hashtags</strong><br/>{hashtags}</p>
          <p><strong>Source</strong><br/><a href='{link}'>{link}</a></p>
          <p><strong>Date</strong><br/>{date}</p>
          <p><strong>Image</strong><br/><img src='{image}' alt='News Image' style='max-width:100%;height:auto;max-height:400px;border-radius:8px;'/></p>
          <div style='margin-top:20px;'>
            <a href='{approve_link}' style='display:block;margin:8px 0;padding:12px 16px;background:#198754;color:#ffffff;text-decoration:none;border-radius:6px;font-weight:bold;'>Approve & Post</a>
            <a href='{redo_story_link}' style='display:block;margin:8px 0;padding:12px 16px;background:#dc3545;color:#ffffff;text-decoration:none;border-radius:6px;font-weight:bold;'>Redo Story</a>
            <a href='{redo_image_link}' style='display:block;margin:8px 0;padding:12px 16px;background:#fd7e14;color:#ffffff;text-decoration:none;border-radius:6px;font-weight:bold;            </a>
          </div>
          """

          payload = {
          payload = {
redo_image_link}' style='displa: senredo_image_link}' style='displa: senredo_image_link}' style='displa: se    "subject": "Approval Required: Daily IT News",
                                                                                                                                                                                                 3/smtp/email" \
            -H "accept: application/json" \
            -H "api-key: $BREVO_KEY" \
            -H "content-type: application/json" \
            -d @payload.json
