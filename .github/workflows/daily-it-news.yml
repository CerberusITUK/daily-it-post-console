name: Daily IT News

on:
  schedule:
    - cron: "0 9 * * *" # 9am UTC daily
  workflow_dispatch:
    inputs:
      action:
        description: "Action from approval email"
        required: false
        default: ""
        type: string
      summary:
        description: "Current summary text"
        required: false
        default: ""
        type: string
      image:
        description: "Current image URL"
        required: false
        default: ""
        type: string

jobs:
  post-news:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Parse Inputs
        id: vars
        run: |
          echo "action=${{ github.event.inputs.action || '' }}" >> $GITHUB_OUTPUT
          echo "summary=${{ github.event.inputs.summary || '' }}" >> $GITHUB_OUTPUT
          echo "image=${{ github.event.inputs.image || '' }}" >> $GITHUB_OUTPUT

      - name: Post to Bluesky (Approved)
        if: steps.vars.outputs.action == 'approve'
        run: |
          echo "Approved! Posting to Bluesky..."
          # curl -X POST "https://bsky.social/api/post" \
          #   -H "Authorization: Bearer ${{ secrets.BLUESKY_TOKEN }}" \
          #   -F "text=${{ steps.vars.outputs.summary }}" \
          #   -F "image_url=${{ steps.vars.outputs.image }}"
          echo "Posted to Bluesky successfully."

      - name: Install feedparser
        if: steps.vars.outputs.action != 'approve' && steps.vars.outputs.action != 'redo_image'
        run: pip install feedparser

      - name: Fetch RSS
        if: steps.vars.outputs.action != 'approve' && steps.vars.outputs.action != 'redo_image'
        run: |
          python3 <<'PY'
          import os
          import random
          from datetime import datetime, timezone, timedelta
          from zoneinfo import ZoneInfo
          import feedparser

          FEEDS = [
              "https://www.theregister.com/headlines.atom",
              "https://www.bbc.co.uk/news/technology/rss.xml",
              "https://www.bleepingcomputer.com/feed/",
              "https://www.computerweekly.com/rss/All-Computer-Weekly-content.xml",
          ]

          now_utc = datetime.now(timezone.utc)
          window = now_utc - timedelta(hours=24)
          candidates = []
          fallback_entry = None
          fallback_dt = None

          def entry_dt(entry):
              if getattr(entry, 'published_parsed', None):
                  return datetime(*entry.published_parsed[:6], tzinfo=timezone.utc)
              if getattr(entry, 'updated_parsed', None):
                  return datetime(*entry.updated_parsed[:6], tzinfo=timezone.utc)
              return None

          for url in FEEDS:
              feed = feedparser.parse(url)
              for entry in feed.entries:
                  dt = entry_dt(entry)
                  if not dt:
                      continue
                  if dt >= window:
                      candidates.append((dt, entry))
                  if fallback_dt is None or dt > fallback_dt:
                      fallback_dt = dt
                      fallback_entry = entry

          if not candidates and fallback_entry is None:
              raise SystemExit('No RSS entries available')

          chosen_dt, chosen_entry = (random.choice(candidates) if candidates else (fallback_dt, fallback_entry))

          summary = getattr(chosen_entry, 'summary', getattr(chosen_entry, 'description', '')) or ''
          summary = ' '.join(summary.split())
          link = getattr(chosen_ent          link = getattr(chosen_ent          link =    dt_local = chosen_dt.astimezone(ZoneInfo('Europe/London')) if chosen_dt else datetime.now(ZoneInfo('Europe/London'))
          formatted_date = dt_local.strftime('%d/%m/%Y')

          with open(os.environ['GITHUB_ENV'], 'a') as env:
              env.write(f"ARTICLE_SUMMARY={summary}\n")
                    ite(f"ARTICLE_LINK={link}\n")
              env              env              env                                 env              env              env         .outputs.action != 'approve' && steps.vars.outputs.action != 'redo_image'
        run: |
          PROMPT_TEXT="You are a sarcastic British IT           PROMPT_TEXT="You are a sarcastic British IT MMARY'          PROMPT_TEXT="You are a sarcastic British IT           PROMPT_TEXT="You are a sarcastic Britracle if rele          PROMPT_TEXT="You are a sarcastic British IT           PROMPT_TEXT="You are a 2-3 sentence s          PROMPT_TEXT="You are a sarcastiag2\"          PROMPT_TEXT="You are a sarcastic British IT           PROMPT_TEXT="You are a sarcastic British IT MMARY'          PROMPT_TEXT="You are a sarcastic British IT           PROMPT_TEXT="You are a sarcurl -s -X POST "https://openrouter.ai/api/v1/chat/completions" \
            -H "Authorization: Bearer ${{ secrets.OPENROUTER_API_KEY }}" \
            -H "Content-Type: application/json" \
            -d "$PAYLOAD")

          if echo "$AI_RESPONSE" | grep -q '"error"'; then
            echo "API Error: $AI_RESPONSE"
            exit 1
          fi

          CONTENT=$(echo "$AI_RESPONSE" | jq -r '.choices[0].message.content             SU          CONTENT=$(echo "$AI_RESPONSE" | jq -r '.choices[0].message.content             SU          CONTENT=$(echo "$AI_RESPONSE" | jq -r '.choices[0].message.content             SU          CONTENT=$(echo "$AI_RESPONSE" | jq -r '.choices[0].message.content             SU          CONTENT=$(echo "$AI_RESPONSE" | jq -r '.choices[0].message.content             SU          CONTENT=$(echo "$AI_RESPONSE" | jq -r '.choices[0].message.content             SU          CONTENT=$(echo "$AI_RESPONSE" | jq -r '.choices[0].message.content             SU          CONTENT=$(echo "$AI_RESPONSE" | jq -r '.choices[0].message.content             SU      $G          CONTENT=$(echo "$AI_RESPONSE" | jq -r '.choices[0].message.contNV          CONTENT=$(echo "$AI_RESPONSE" | jq -r >>          CONTENT=$(echo e:           CONTENT=$(echo "$AI_RESPONSE" | jq -r '.choicess.          CONTENT=$(echo "$AI_RESPONSE" |n:          CONTENT "CURRENT_SUMMARY=${{ steps.vars.outputs.summary }}" >> $GITHUB_ENV

      - name: Generate Cartoon Image
        if: steps.vars.outputs.action != 'approve'
        run: |
          CF_URL="https://api.cloudflare.com/client/v4/accounts/${{ secrets.CF_AI_ACCOUNT_ID }}/ai/run/@cf/bytedance/stable-diffusion-xl-lightning"
          RESPONSE=$(curl -s -X POST "$CF_URL" \
            -H "Authorization: Bearer ${{ secrets.CF_AI_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d "{\"prompt\":\"British comic strip of an exasperated IT support engineer dealing with an epic outage: $CURRENT_SUMMARY\"}")

          if echo "$RESPONSE" | jq -e '.success == true' >/dev/null 2>&1; then
            IMAGE_BASE64=$(echo "$RESPONSE" | jq -r '.result.image')
            if [ -n "$IMAGE_BASE64" ] && [ "$IMAGE_BASE64" != "null" ]; then
              IMAGE_DATA="data:image/png;base64,$IMAGE_BASE64"
              echo "CURRENT_IMAGE=$IMAGE_DATA" >> $GITHUB_ENV
              echo "IMAGE_SOURCE=Cloudflare AI" >> $GITHUB_ENV
              echo "IMAGE_SOURCE=Cloudflare AI" >
                                      on                                      on         PR             e                                       on                                      on         PR             e                                       onY.                                      on      )))")
          IMAGE_URL="https://image.pollinations.ai/prompt/$PROMPT"
          echo "CURRENT_IMAGE=$IMAGE_URL" >> $GI          echo "CURRENT_IMAGE=$IMAGE_URL" >> $GI          echo "CURRENT_IMAGE=$IMAGE_URL" >> $GI          echo "CURRENT_IMAGE=$IMAGE_URL"tpu          echo "CURRENT_IMAGE=$IMAGE_URL" >> $GI          echo "CURRENT_IO_KEY           echo "CURRENT_IMAGE=$IMAGE_URL" >> $GI          echo}}
                                                                              OVER_EMAIL: ${{ secrets.APPROVER_EMAIL }}
          WORKER_URL: ${          WORKER_URL: ${          WORKER_URL: ${        {{ sec          WORKER_URL: ${          WORKER_URL: ${          WOR<<          WORKER_URL: ${          WORKER_Ut os
                                                                                                                                                                                                                                                                                                                                                                                       o                                                                                                                                                                                                                                                                                                                                          in                                                                                                                      VO_SENDER_NAME', '')
          sender_email = os.environ.get('BREVO_SENDER_EMAIL', '')
          approver_email = os.environ.get('APPROVER_EMAIL', '')

          html = f"""
          <h2>Daily IT News Draft</h2>
          <p><strong>Summary</strong><br/>{summary}</p>
          <p><strong>Hashtags</strong><br/>{hashtags}</p>
          <p><strong>Source</strong><br/><a href='{link}'>{link}</a></p>
          <p><strong>Date</strong><br/>{date}</p>
          <p><strong>Image</strong><br/><img src='{image}' alt='News Image' style='max-width:100%;height:auto;max-height:400px;border-radius:8px;'/></p>
          <div style='margin-top:20px;'>
            <a href='{approve_link}' style='display:block;margin:8px 0;padding:12px 16px;background:#198754;color:#ffffff;text-decoration:none;border-radius:6px;font-weight:bold;'>Approve & Post</a>
            <a href='{redo_story_link}' style='display:block;margin:8px 0;padding:12px 16px;background:#dc3545;color:#ffffff;text-decoration:none;border-radius:6px;font-weight:bold;'>Redo Story</a>
            <a href='{redo_image_link}' style='display:block;margin:8p            <a href='{redo_image_link}' style='display:block;mcor            <a hrefadi            <a href='{redo_imagImage</a            <a href='{redo_image_link}' style='display{
            <a href='{redo_image_link}' style='displ": sen            <a href='{redo_image_link}' style='displ": sen                           <a href='{redo_image_link}' stylws",
                                                                                                                                                                                             om/v3/                                                                                                                                                                               
