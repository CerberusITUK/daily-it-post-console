--- .github/workflows/daily-it-news.yml
+++ .github/workflows/daily-it-news.yml
@@ -107,17 +107,17 @@
       - name: Generate AI Summary
         if: steps.vars.outputs.action != 'approve' && steps.vars.outputs.action != 'redo_image'
         run: |
-          PROMPT_TEXT="You are a sarcastic British IT support columnist. Given the news summary '$ARTICLE_SUMMARY', craft a witty British-leaning recap that highlights the epic IT fail and includes at least one pun. Respond ONLY with JSON matching: {\"summary\": \"<2-3 sentence summary>\", \"hashtags\": [\"#tag1\", \"#tag2\", \"#tag3\"] }."
+          PROMPT_TEXT="You are a deeply cynical, witty British IT professional (like BOFH). Read this news summary: '$ARTICLE_SUMMARY'. Write a highly sarcastic 2-3 sentence recap mocking the utter incompetence, poor decisions, or epic IT fail involved, using heavy British slang and at least one terrible pun. You MUST output ONLY valid JSON matching exactly this format: {\"summary\": \"<your cyn+          PROMPT_TEXT="You a\"+          PROMPT_TEXT="You are a o not +          PROMPT_TEXT="You are, backticks, or text outside the JSON."
           PAYLOAD=$(jq -n           PAYLOAD=$(jq -n           PAYLOAD=$(jq -n           PAYLOAD=$(jq -n           PAY "use         nt: $ms           PAYLOAD=$(jq -n           PAYLOAD=$(jq -n           PAYLOAD=$(jq -n           PAYLOAD=$(jq -n           PAY "use         nt: $ms           PAYLOAD=$(jq -n           PAYLOAD=$(jq -n           PAYLOAD=$(jq -n           PAYLOAD=$(jq -n           PAY "use         nt: $ms           PAYLOAD=$(jq -n           PAYLOAD=$(jq -n           P     echo "API Error: $AI_RESPONSE"
             exit 1
           fi
 
-          CONTENT=$(echo "$AI_RESPONSE" | jq -r '.choices[0].message.content')
+          CONTENT=$(echo "$AI_RESPONSE" | jq -r '.choices[0].message.content' | sed -n '/{/,/}/p')
           SUMMARY_TEXT=$(echo "$CONTENT" | jq -r '.summary' 2>/dev/null || true)
           HASHTAGS_LINE=$(echo "$CONTENT" | jq -r '.hashtags | join(" ")' 2>/dev/null || true)
